#include<stdio.h>
main()
{
	int a,b,c;
	if (a>b && a>c)
	printf("Enter the values if a b and c");
	scanf("%d%d%d",&a,&b,&c);
	{
		printf("a is largest");
	}
      else if (b>a && b>c)
	{
		printf("b is largest");
	}
      else if ("c is the largest")
	
	
}
